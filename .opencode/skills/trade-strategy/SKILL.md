---
name: "trade-strategy"
description: 当用户要求"快捞参数""快捞策略""快捞止盈止损""冷却状态""检查策略逻辑""查看策略"时必须使用。本 skill 管理快捞策略执行与风控（浮动锁仓、止损、冷却、趋势过滤），当前仅有的交易策略为快捞策略。
---

# 交易策略层 — 快捞策略（唯一策略）

基于快捞策略的自动化交易执行与风险管理。监测全场 USDT 永续合约，15分钟涨跌幅>6% 触发追涨杀跌，采用浮动利润锁仓策略让利润奔跑。当前系统仅支持快捞策略，无传统主仓策略。

## 适用场景

- 用户要求"快捞参数是多少"、"快捞的止盈止损比例"
- 用户要求"看看当前快捞状态"、"冷却时间过了没"
- 用户要求"现在策略是怎么跑的"
- 用户要求"调整一下快捞参数"
- 系统自动调用（快捞循环每2分钟触发）

## 安全约定

- 持仓中禁止同币种开新单
- 止损后同币种进入冷却（`is_cooling()`）
- 单笔大亏损（>20 USDT）冷却延长至60分钟
- 开仓前检查 K 线趋势过滤（做多仅 up，做空仅 down）

## 输入输出

输入：市场快照（币种价格、15分钟涨跌幅）

输出：
- 开仓结果：`{action, symbol, quantity, price, order_id}`
- 持仓风控：止损检查 / 浮动锁仓检查 / 平仓
- 冷却状态：`{cooling: true/false, remaining_minutes}`

## 推荐命令

```python
from utils.trade.fast_trader import try_fast_open, try_fast_short, check_fast_position

# 检查快捞持仓风控
check_fast_position()
```

## 文件位置

| 文件 | 说明 |
|------|------|
| `utils/trade/fast_trader.py` | 快捞策略执行：开多/开空/持仓风控/浮动锁仓 |

## 导出函数

### fast_trader.py

| 函数 | 说明 |
|------|------|
| `try_fast_open(symbol, price, prev_price)` | 快速开多：10%余额，5x杠杆，15min涨>6%触发 + K线up趋势 |
| `try_fast_short(symbol, price, prev_price)` | 快速做空：10%余额，5x杠杆，15min跌>6%触发 + K线down趋势 |
| `check_fast_position()` | 检查快捞持仓风控：止损 / 浮动锁仓 / K线动态回撤平仓（含分批+减量重试）|

## 执行流程

### 快捞监测流程（每2分钟，由 `market/monitor.py` 的 `run_fast_monitor()` 触发）

1. **检查持仓风控**：调用 `check_fast_position()` 检查已有快捞仓位的止损/锁仓
2. **扫描开仓条件**：遍历所有币种，检查 15min 涨跌幅 > 6%
3. **K线趋势过滤**：做多仅趋势=up，做空仅趋势=down
4. **冷却检查**：同币种是否在冷却期内
5. **执行开仓**：调用 core/order.py 下单（10%余额，5x杠杆）
6. **开仓减量重试**：数量过大(-4005)时自动减10%重试，最多5次

### 持仓风控流程（每次快捞循环）

1. **止损检查**：亏损 ≥ 1.5%（保证金）→ 全平，进入冷却
2. **浮动锁仓**：盈利 ≥ 10% 启动锁仓（锁+2%），之后每+5%锁+2%
3. **利润≥30%时**：转为K线1h振幅×趋势系数×4动态回撤
4. **平仓分批**：数量超限额时自动分批+减量重试
5. **无持仓**：跳过风控

## 快捞参数

| 参数 | 值 | 说明 |
|------|-----|------|
| 监测频率 | 每2分钟 | 扫描500+USDT币种 |
| 做多触发 | 15min涨幅>6% + K线趋势up | 对比15分钟前快照 |
| 做空触发 | 15min跌幅>6% + K线趋势down | 对比15分钟前快照 |
| 仓位比例 | 10%余额 | `FAST_MARGIN_RATIO=0.10` |
| 杠杆倍数 | 5x | `FAST_LEVERAGE=5` |
| 止损 | -1.5%（保证金） | 始终有效 |
| 起始锁仓 | 盈利+10% | 锁仓线+2%（回撤到+2%平） |
| 阶梯锁仓 | 利润<30%：固定每+5%锁+2% | 利润≥30%：K线动态回撤 |
| 最低成交额 | ≥50万USDT | 排除流动差的币种 |
| 冷却时间 | 止损后15分钟 | 大亏损(>20U)冷却60分钟 |
| 可同时持多币种 | 是 | 币种之间互不冲突 |

## 浮动锁仓策略

```
第一阶段（利润 < 30%）：固定阶梯锁仓
  盈利+10% ──► 锁仓线+2%（保底2%利润）
  盈利+15% ──► 锁仓线+4%（+5%→+2%）
  盈利+20% ──► 锁仓线+6%
  盈利+25% ──► 锁仓线+8%
    ⋮              ⋮

第二阶段（利润 >= 30%）：K线动态回撤（1h振幅）
  获取最近6根1小时K线 → 算振幅(高-低)/价
  强趋势(up/down) → 趋势系数2.0 → 允许更大回撤不轻易下车
  横盘(sideways) → 趋势系数1.0 → 收紧回撤落袋为安
  回撤 = 振幅 × 趋势系数 × 4，保底5%回撤空间
  锁仓线 = 最高盈利 - 回撤，保底10%
```

- **不加仓、不减仓** — 固定10%余额+5x杠杆，进去就持有不动
- **止损 -1.5%** — 始终有效，方向错了第一时间跑
- **两阶段锁仓** — 利润<30%用固定阶梯（每+5%锁+2%），利润≥30%用1小时K线振幅动态回撤

## 冷却机制

| 条件 | 冷却时间 |
|------|---------|
| 正常止损 | 15分钟 |
| 单笔亏损 > 20 USDT | 60分钟 |
| 到期自动清理 | 每次开仓前清理过期记录 |

## 质量标准

- 开仓前必须检查所有前置条件（冷却/持仓/K线趋势过滤）
- 止损 -1.5% 始终有效
- 浮动锁仓：+10%启动，每+5%盈利锁仓线+2%，≥30%用K线动态
- 快捞仓位可同时持多个币种，互不影响
- 开/平仓数量超限时自动减量重试
